import { StyleSheet } from "react-native";

export const stylesReportScreen = StyleSheet.create({
    inputField: {
        color: '#000',
        fontSize: 14,
        borderRadius: 0,
        borderWidth: 0,
        elevation: 0,

    },
    inputFieldIOS: {
        borderBottomColor: '#000',
        borderBottomWidth: 2,
        paddingBottom: 4,
    },
})